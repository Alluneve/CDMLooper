# CDMLooper

CDMLooper extends World of Warcraft's Cooldown Manager alerts with optional repeating sound notifications.
Looping can be enabled per alert and configured with a custom repeat interval.
Repeated alerts stop when the tracked spell is used or when combat ends.
An optional global setting prevents looped alert sounds from overlapping.
CDMLooper integrates directly into Blizzard's existing Cooldown Manager UI.
